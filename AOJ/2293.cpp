#include <bits/stdc++.h>
using namespace std;
#define repi(i,a,b) for(int i = (int)(a); i < (int)(b); i++)
#define rep(i,n) repi(i,0,n)

int solve()
{
    
}

void input()
{
    scanf("%d", &n);
    rep(i,n) scanf("%d%d", h+i, w+i;);
}

int main()
{
    input();
    printf("%d\n", solve());
    return 0;
}
