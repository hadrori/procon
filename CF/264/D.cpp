#include <iostream>
#include <string>
using namespace std;
#define repi(i,a,b) for(int i = int(a); i < int(b); i++)
#define rep(i,a) repi(i,0,a)
typedef long long ll;
string s, t;
int n, m;
ll mul;

int main(){
    cin >> s >> t;
    n = s.size(); m = t.size();
    return 0;
}
